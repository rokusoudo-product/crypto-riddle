# scripts/validate_scenarios.py / scripts/validate_terms.py の実行に必要な依存(暫定)。
# T001(リポジトリ足場)完了後、本番のシナリオ/用語データ検証は plan.md §4 の
# YAML -> zod 検証 -> JSON パイプライン(T010, Node/TypeScript)に一本化する想定。
# それまでの間、これらのスクリプトは Python + venv で単独実行できる。
#
# 使い方:
#   python3 -m venv .venv-validate
#   .venv-validate/bin/pip install -r scripts/requirements.txt
#   .venv-validate/bin/python scripts/validate_scenarios.py
#   .venv-validate/bin/python scripts/validate_terms.py

pyyaml==6.0.3
jsonschema==4.26.0

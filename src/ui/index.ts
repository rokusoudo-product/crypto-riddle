// src/ui — React コンポーネント（表示と入力操作のみ）。
// ui/ は core/ を import してよい（依存は常に ui → core の一方向、plan.md §2）。
// T011: ルーティングと8画面の骨格。AppRoutes を root（src/main.tsx）から読み込む。
export { AppRoutes } from '@/ui/routes'

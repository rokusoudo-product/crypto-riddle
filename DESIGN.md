# DESIGN.md — crypto-riddle

> 全プロジェクト共通の `DESIGN_STANDARDS.md` を上位規範とし、本ファイルはプロジェクト固有の決定を記録する。
> UI 実装は本ファイルのトークン・ルールに従う（カラーコード・サイズの直書き禁止）。
> 由来: 画面プロトタイプ（Opus 5 / Claude Design・design-sync-testbed）を下敷きに、DESIGN_STANDARDS へ準拠させたもの（#7）。

## 基本方針

- 対象プラットフォーム: **Web アプリ（スマホブラウザ対応）**（spec 決定）
- 準拠ガイドライン: **WCAG 2.1 AA**（必須）
- 使用コンポーネントライブラリ（第一候補）: **Tailwind CSS + shadcn/ui**（最終確定は plan.md／ゲート②の技術スタック決定時）
- トーン&マナー: **サイバーパンク／近未来のセキュリティ対応アドベンチャー**（2026-09-14 改訂・#132）。旧「静かで硬派な推理アドベンチャー」は撤回。世界観は「ネオンに縁取られた対策室で事件と向き合う緊張感」。**没入と可読性を優先する姿勢は変えない**（派手な演出よりも読みやすさ・状態の伝わりやすさを優先する原則は引き継ぐ）。キャラクターの関係性・人物像（候補C・クールなプロ同士の信頼型）自体は変更しない。変わるのは画面の配色・演出トーンのみ（`docs/characters.md` 参照）。
- アートディレクション: **単一のダーク文脈に統一**（旧「ライト文脈（タイトル/メニュー/図鑑）＝温かいクリーム基調」「ダーク文脈（導入/探索/解決/失敗解説/結果）＝暖色寄りの暗色」の2文脈使い分けは廃止）。**すべての画面**（①タイトル／②マップ選択／③導入／④探索／⑤解決／⑦結果／⑧カード図鑑）を、下記カラートークンのダーク基調（ダークブルー／パープル×ネオンブルー・ネオングリーン）で統一する（**秘書推奨・代表確認待ち #132**。未解決の質問1への回答）。
  - **OS のライト/ダークモード追従について（秘書推奨・代表確認待ち #132）**: 本作はネオン配色を活かすゲーム世界観であり、OS 側がライトモードでも**常にダークテーマで固定表示**し、OS 設定には追従しない。`DESIGN_STANDARDS.md`§4「ダークモード対応を初期設計に含める」は、本プロジェクトが最初からダーク基調1本であることで満たすと整理する（ライトテーマを別途用意しない）。この結果、下記カラートークン表は**値1列**（旧「ライト」「ダーク」2列）に簡略化する。

## カラートークン

アクセントは**ネオンブルー1色**（60-30-10：ベース＝ダークブルー／パープルのニュートラル／サブ＝濃ニュートラル／アクセント＝ネオンブルー）。**ネオングリーンはアクセントではなく状態色**（success・XP 等の状態表示専用）として扱い、`DESIGN_STANDARDS.md`§4「アクセント色は1色」に準拠させる（**秘書推奨・代表確認待ち #132**。未解決の質問2への回答。逸脱ではないことを明記する）。**ネオングリーンはボタン・リンク・フォーカスリングなど操作系の強調には使わない**（状態表示専用の制約を守ることで60-30-10の「アクセント1色」を保つ）。

全ペアの WCAG 2.1 AA コントラスト検証結果は `docs/design-contrast.md`（#13 を本 Issue に含めて検証・完了）を参照。以下の hex は**確定値**（検証済み。旧「仮値」の但し書きは撤回）。

| トークン | 値 | 対応する CSS 変数（`src/index.css`） | 用途 |
|---|---|---|---|
| background | #0A0C18 | `--background` | 画面背景（ダークブルー基調） |
| surface | #11142A | `--card` / `--popover` | カード・パネル地 |
| surface-2 | #1A1F3D | `--secondary` / `--muted` / `--accent` / `--input` | 二次面・入力枠・中央選択パネルのボタン地（パープル寄りにわずかに明度を上げた面） |
| text-primary | #EAF0FF | `--foreground` / `--card-foreground` / `--popover-foreground` | 本文 |
| text-secondary | #A6B0D6 | `--muted-foreground` | 補助テキスト |
| border | #6870AD | `--border` / `--input`（枠線用途） | 枠線・区切り（非テキスト3:1） |
| primary（accent） | #4FD8FF | `--primary` / `--ring` | 主要アクション・強調・フォーカス（ネオンブルー。60-30-10のアクセント） |
| primary-foreground | #0A0C18（=background） | `--primary-foreground` | primary 塗りボタンの上に載せる文字色 |
| success | #4CFFA8 | `--success`（新規） | 状態色（正答・XP・クリア等。操作系には使わない） |
| success-foreground | #0A0C18（=background） | `--success-foreground`（新規） | success 塗り要素の上に載せる文字色 |
| warning | #FFC24C | `--warning`（新規） | 事件・注意・教育的失敗 |
| warning-foreground | #0A0C18（=background） | `--warning-foreground`（新規） | warning 塗り要素の上に載せる文字色 |
| error | #FF5C7A | `--error`（新規。`--destructive` のエイリアスでもよい） | 誤答・破壊的操作 |
| error-foreground | #0A0C18（=background） | `--error-foreground`（新規） | error 塗り要素の上に載せる文字色 |
| info | #8FA6FF | `--info`（新規） | 補足情報（primary の青紫寄りバリエーションで区別） |
| hotspot-highlight | oklch(0.749 0.202 22.86)（hex 近似 #FF5A4E。#71・T045 の既存ダーク値をそのまま維持） | `--hotspot-highlight` | 探索ホットスポット□マーカーの枠線（危険操作系の warning/error とは別扱い。**維持・変更なし**。視認性のため赤系を継続する代表指定） |
| speaker-frame-white | oklch(1 0 0) | `--speaker-frame-white` | 会話中の立ち絵カードを囲む話者の枠（**白のまま維持・代表決定 2026-09-14 を継続**。未解決の質問5への回答。既定値） |
| speaker-frame-neon-blue | #4FD8FF（primary と同値） | `--speaker-frame-neon-blue`（新規・併記） | 話者の枠のネオンブルー案。**既定は white のまま**、`SPEAKER_FRAME_COLOR` 定数で white↔neon-blue を切替可能なトークンとして併記する |
| speaker-frame-black | oklch(0 0 0) | `--speaker-frame-black`（既存・**使用禁止**） | ダーク統一の背景では成立しない（`docs/design-contrast.md` で NG 確認）。切替候補からも外し、`SPEAKER_FRAME_COLOR` は `white \| neon-blue` の2値のみ許可する |

- すべて CSS 変数（テーマトークン）経由で参照する。カラーコードの直書きは禁止（DESIGN_STANDARDS.md §4）。
- **旧→新トークン対応（#13 受け入れ基準「変更前後の値と理由」）**: primary はゴールド（#D3AC57系）→ネオンブルー（#4FD8FF）、background/surface はクリーム/焦げ茶系→ダークブルー系、success/warning/error/info は暖色寄りの彩度低めの配色→サイバーパンク基調の高彩度配色に**色相ごと変更**。理由: 2026-09-14 代表決定（#132 背景参照）でトーン自体をサイバーパンクへ全面変更したため、ゴールド1色・暖色暗色という前提が置き換わった。#13 で予定していた「静かで硬派」「ゴールド1色」トーンを維持したままの微調整は行わず、本 Issue でトーン変更ごと確定値を出す。
- success/warning/error/info は現状 `src/index.css` に CSS 変数として未配線（DESIGN.md 旧「danger（教育的フィードバック）の配色」注記のとおり）。**#133 でトークン配線と同時に新規変数として追加する**（`--destructive` は既存 shadcn 変数のため、error は「新規 `--error` を追加」「既存 `--destructive` を流用」のいずれでも可とし、#133 の実装判断に委ねる。どちらにせよトークン名は本表のとおり）。

## 半透明（ガラス風）パネル

会話ウィンドウ・解決⑤の中央選択パネル・アクションシート・カードドロワーは、背景シーンがうっすら透けるガラス風の半透明パネルにする（2026-09-14 代表決定・#132 背景参照）。

- **パネル地の色**: `surface` トークン（#11142A）。
- **最小不透明度トークン（新規・`--glass-panel-min-alpha`）**: **0.81**。
- **導出方法（背景画像に依存しない検証）**: 実際の背景画像ごとに実測すると画像差し替えのたびに再検証が必要になるため、**パネル色を最小不透明度で「純白」と「純黒」それぞれの上に合成した2色**を作り、**両方に対して本文（text-primary・text-secondary）が 4.5:1 を満たすかで判定する**規則にする。これにより、どんな明るさの背景画像の上でも文字が読める不透明度を保証できる。**明るい対策室背景（導入③・#139）の例外にもこの規則で耐える**（下記「対策室の例外」参照）。
  - 0.81 は「0.50 から 0.01 刻みで、text-primary/text-secondary が純白・純黒合成の両方で 4.5:1 を満たす最小値+0.01（丸め誤差の安全マージン）」として `scripts/check-contrast.mjs` が自動算出した値（手動の当て推量ではない）。算出根拠・実測結果は `docs/design-contrast.md` を参照。
  - 0.81 は完全な透過ではなく、背景がうっすら透ける程度の「ガラス風」の下限として扱う。これより下げると text-secondary が明るい背景の上で 4.5:1 を割るため使用しない。
- **ぼかし量トークン（新規・`--glass-panel-blur`）**: **12px**（`backdrop-filter: blur(12px)`）。ぼかしはコントラスト計算に寄与しないため、コントラストは上記の不透明度のみで担保する。
- **適用先**: 会話ウィンドウ・解決⑤の中央選択パネル・アクションシート（下記「探索シーン」節参照）・カードドロワー。
- **適用しない（不透明を維持）**: 右上のボタン群（「ヒント確認」「調査ポイント一覧」）は発見性のため従来どおり不透明のまま（代表指定・維持。下記「探索シーン」節参照）。
- **実装確認手順（#133/#139 の受け入れ基準「#132 の手順」に対応）**: 背景画像ごとの実測は不要（上記の合成検証で担保済み）。実装時は、DevTools で対象パネルの computed `background-color` の alpha が `--glass-panel-min-alpha`（0.81）以上であること、かつ本文の色が `text-primary`（#EAF0FF）／`text-secondary`（#A6B0D6）のトークン値そのものであることの2点を確認すれば足りる（`docs/design-contrast.md` の検証済みの組み合わせから外れていないかの確認）。

## タイポグラフィ

- 見出し・世界観テキスト: **Noto Serif JP（明朝）**
- 本文・操作UI: **Noto Sans JP（ゴシック）**
- スケール: **12 / 14 / 16 / 20 / 24 / 32**（本文16、補助12）。行間 本文 1.5〜1.7
- プロジェクト固有: **タイトル・演出見出しは display スケール（40 / 48 / 64）を許可**（世界観優先。本文・UI には使わない）

## 余白・レイアウト

- **8ptグリッド**準拠（例外は 4 のみ）
- モバイル縦を第一。**背景を持つ画面（導入・探索・解決）ではプレイ枠＝背景の箱**（下記「探索シーン」節「背景の箱」参照・#119。**解決⑤は「解決へ進む」を押した時点で表示していた探索シーンの背景をそのまま使う**＝新しい画像は作らない・代表決定 2026-09-13・実装は #124。詳細は下記「会話フレーム」節参照）とし、会話ウィンドウ・カード領域は箱の幅の内側に収める。**背景を持たない画面（タイトル・マップ選択・結果・カード図鑑など）はプレイ枠を中央寄せ**、コンテンツ最大幅は 720〜960px 目安（モバイルは全幅）
- **背景を持つ画面はコンテナの最大幅を適用しない（代表決定2026-09-14・#124）**: 背景の箱を画面いっぱい（100dvw×100dvh）に表示するため、上記の 720〜960px 幅制約・ページ縦スクロールは適用しない（`ScreenContainer` の `variant="immersive"`）。画面見出し（h1「導入」「探索」「解決」）は視覚的には出さず `sr-only` にする。シーンタブ・「ヒント確認」「調査ポイント一覧」・SKIP・「解決へ進む」・導入の会社名と説明（半透明の surface パネル）は、いずれもページ上部の余白ではなく**箱の中に重ねて**表示する（会話フレームの立ち絵・会話ウィンドウと重ならない位置に配置）。
- 1画面1目的。主要アクションは1画面に1つ（例: 解決＝現在の問いへの「回答」。相談・カード閲覧は補助操作）

## 画面一覧と状態

各画面に DESIGN_STANDARDS の**4状態（通常／ローディング／空／エラー）**を実装する。

| 画面 | 目的 | 主要アクション | 4状態の特記 |
|---|---|---|---|
| ①タイトル | 起動・導線 | つづきから | ローディング=セーブ読込／空=セーブ無し→「はじめから」／エラー=読込失敗表示。左下に作成時点の注意書き（#81） |
| ②マップ選択 | 事例選択 | マップを選ぶ | ローディング=進捗取得／空=未解放の案内／エラー=取得失敗 |
| ③導入 | 事件の前提提示 | 画面クリックで進行・SKIP | **会話フレーム**を使用（**左右2枠の入れ替わり方式**。探索④・解決⑤と共通。#108）。エラー=シナリオ読込失敗 |
| ④探索 | 手がかり収集 | **背景シーン上のオブジェクト（PC/人物/書籍）をタップ**→調査→カード獲得 | **背景シーン＋ホットスポット**（#52）。PC操作メニュー（ログ取得／電源を落とす等）。人物の証言は**会話フレーム**。背景に頼らない**一覧表示**を併設。ローディング=判定中／空=未調査／エラー |
| ⑤解決 | 攻撃手段の特定・防衛（**会話モード**） | 問いに2〜3択で答える／相談／手持ちカードを見る | 背景の箱の**中央の選択パネル**（問い＋選択肢＋相談ボタン）に載せる。下部の**会話ウィンドウは台詞（タイプライター）専用**、会話ログは置かない（2026-09-14 代表決定・#132。旧「会話フレーム上に選択肢・相談」は撤回）。通常／誤答フィードバック（外すたび解説が深まる）／エラー。**単一解・厳密一致は問い単位で維持**（#7・#42）。**背景は直前の探索シーンをそのまま使う**（新規背景は作らない・代表決定・#119/#124） |
| ⑥失敗解説 | ~~教育的失敗の解説~~ | — | **廃止（#42）**。教育的失敗は⑤解決の会話モード内（誤答時に深まる解説）へ統合 |
| ⑦結果 | 判定・用語・出典 | 次へ／図鑑 | 獲得XP（誤答・相談による減算を反映）・累計XP・出典を表示。ローディング=保存中／エラー=保存失敗 |
| ⑧カード図鑑 | 収集用語の閲覧 | 分野で絞る | 空=未収集の案内／ローディング |

## 会話フレーム（共通コンポーネント・#42 で導入）

導入（③）・探索の会話（④）・解決の会話モード（⑤）で共通して使う、立ち絵つきの会話演出。コンセプトモック（Obsidian タスク `Tasks/po_agent/crypto-riddle/` に保持。**Public リポジトリに URL を載せない**）で代表確認済み（2026-09-10）。

- **レイアウト**: 画面下部に**会話ウィンドウ**、その上に**左右2枠**の立ち絵。**導入③・探索④・解決⑤のすべてで共通**（#108。S1 実装台本レビュー第1回・2026-09-13 代表フィードバックを受けての改訂。旧仕様（#100）は「探索④・解決⑤は霧島＝左・橘＝右の固定2枠／導入③のみ小鳥遊を加えた3枠の対策室レイアウト」だったが、**全パート左右2枠の入れ替わり方式**に統一し、固定枠・3枠の記述はすべて本節の内容に置き換える）。会話ウィンドウは**半透明（ガラス風）パネル**（上記「半透明（ガラス風）パネル」節のトークン）＋上辺に primary（ネオンブルー）のアクセント（**2026-09-14 代表決定・#132。旧「primary（ゴールド）」は撤回**）。
- **左右2枠の入れ替わり方式（#108）**: 話者が誰であっても画面には常に左右2枠しか使わない。枠の位置そのものは動かさず、色（カラー／グレー）と中身の入れ替えで「直前に話した人」「いま話している人」を示す。
  - 最初に話した人は**左**に入る（右は空のまま）。
  - **画面にいる人が話す**: その人の枠を**カラー**にし、もう一方を**グレー**にする。
  - **画面にいない人が話す**: **直前に話した人ではない方の枠**（空いていればそこ）にその人を入れ、直前に話した人の枠を**グレー**にする。
  - 例: A が話す（左A・右なし）→ B が話す（左Aグレー・右B）→ A が話す（左A・右Bグレー）→ C が話す（左Aグレー・右C）。
  - **NPC が話すとき**: 枠は動かさず**両方グレー**にし、NPC 名を名前箱に出す（下記「名前箱」節「NPC が話すとき」・「探索シーン」節の NPC 直接発話と同じ）。NPC の次に支援役（霧島／橘／小鳥遊）が話すときは、**NPC の前に話していた支援役**を「直前に話した人」として扱う（入れ替え判定に使う。NPC 自体は枠に入らないため）。
  - **並びのリセット**: **導入の開始**・**探索の会話1つ（調査結果・危険操作など1つのオーバーレイ）の開始**・**解決の開始**のたびに、左右の並びを初期化する（次に話した人が再び左から始まる）。**解決は問1→問2に進んでも並びを保つ**（問いをまたいではリセットしない）。
  - **小鳥遊**: 旧「導入のみ中央後方やや小さめの3枠（対策室レイアウト）」は廃止し、導入・探索・解決のすべてで他の2名と同じ2枠の決まりに従う（画面にいる／いないの扱いも同様。登場範囲自体＝①導入・⑦結果のみは変更なし。`docs/scenario_schema.md` §2.6「小鳥遊ガード」）。

  導入（9行）に当てはめた見え方（例。**#110（PR #118）で単体テスト化済み・#124 に引き継ぐ**）:

  | 行 | 話者 | 左 | 右 |
  |---|---|---|---|
  | 1 | 小鳥遊 | 小鳥遊 | なし |
  | 2 | 小鳥遊 | 小鳥遊 | なし |
  | 3 | 霧島 | 小鳥遊（グレー） | 霧島 |
  | 4 | 小鳥遊 | 小鳥遊 | 霧島（グレー） |
  | 5 | 霧島 | 小鳥遊（グレー） | 霧島 |
  | 6 | 橘 | 橘（小鳥遊と入れ替わり） | 霧島（グレー） |
  | 7 | 橘 | 橘 | 霧島（グレー） |
  | 8 | 霧島 | 橘（グレー） | 霧島 |
  | 9 | 小鳥遊 | 小鳥遊（橘と入れ替わり） | 霧島（グレー） |

- **立ち絵の拡大（#108・寸法は #119 で比率指定に改訂）**: 背景の箱（下記「探索シーン」節「背景の箱」参照）の**上に大きく乗る**大きさまで拡大する。**透過・切り抜きはしない**（アセット表のとおり無地背景のまま、現行のカード表示を拡大するだけ。3:4 の比率は維持）。**#108 で定めた固定 px（デスクトップ 幅240×高さ320px／モバイル 幅120×高さ160px）は撤回**し、**背景の箱の高さに対する比率**で決める（初期値は S1 試作で調整する前提。実装は #124）。
  - 目安: **横長の箱（16:9）では箱の高さの45〜50%程度**、**縦長の箱（9:16）では箱の高さの30〜35%程度**を立ち絵の高さとする（3:4 比率から幅を算出）。固定値ではなく、S1 試作（#124）で会話ウィンドウ・選択肢ボタンとの重なりを見ながら微調整する前提の目安値。
  - 立ち絵は**背景の箱の左右の端**に置く。**立ち絵の下端に名前箱、その下に会話ウィンドウ**という縦の積み順にする（**2026-09-14 改訂・#132**。旧「会話ウィンドウの上端に接する」は、名前箱を立ち絵の下に置く決定（下記「名前箱」節）と両立しないため撤回し、本節の積み順に置き換える）。**右上のボタン群（ヒント確認・調査ポイント一覧）とは重ねない**。左右2枠・名前箱を並べても会話ウィンドウ本体・中央の選択パネル（解決⑤）・相談ボタンなど操作要素の領域とは重ならない配置とする。
- **台詞送り（#108）**: 導入③の「タップで進行」ボタンは廃止し、探索④・解決⑤と同じく**画面のどこをクリック/タップしても次の行に進む**（Enter／Space でも送れる）。SKIP、およびタイプライター送出中のクリックで全文表示にする挙動（下記「タイプライター表示」）は維持する。
- **発話者の強調**: いま話している人の枠は**フルカラー**、もう一方の枠（直前に話した人）は**グレーアウト**（グレースケール＋輝度・不透明度を下げる）。NPC が話すときは両方の枠をグレーアウトする。枠に入っている人は、画面にいない人が話して入れ替わるまで表示し続ける（入れ替えの決まりは上の「左右2枠の入れ替わり方式」）。**色（グレーアウト）だけに頼らず、名前箱のテキスト**でも発話者を示す（WCAG 1.4.1）。（経緯: #42 は「霧島・橘の2名固定で直前話者をグレーアウト」、#100 は導入の3枠化に合わせて「発話者以外は全員グレーアウト・登場中の立ち絵は消えない」としたが、#108 で全パートを左右2枠の入れ替わり方式に統一し、3人目が話すと1人が入れ替わりで画面から外れるようになったため、2枠を前提にした本記述に置き換えた）
- **話者の枠（色・#119/#132）**: いま話している人の立ち絵カードは、上記フルカラー表示に加えて**白の枠線**で囲む（**白を維持・代表決定 2026-09-14 を継続**。未解決の質問5への回答＝秘書推奨。`speaker-frame-white` トークン。#124）。**ネオンブルー案（`speaker-frame-neon-blue`）をトークン行として併記し、`SPEAKER_FRAME_COLOR` 定数で white↔neon-blue を切替可能なまま残す**（既定は white）。**黒案（`speaker-frame-black`）はダーク統一の背景では成立しないため使用禁止とし、切替候補から外す**（`docs/design-contrast.md` で NG 確認。旧「黒案のトークン・切替の仕組みは残す」は撤回）。グレーアウト・名前箱による発話者表示（上記「発話者の強調」）はそのまま維持する。
- **導入③の背景・表情フォールバック（#100・背景は #119 で対策室の新規背景に変更）**:
  - **背景**: 導入(③)は **`bg-sl-office`（法務SLシナリオの自社執務室背景）の流用をやめ**（2026-09-12 承認の流用方針を #119 で撤回）、対策室の新しい背景 **`bg-hq-taskforce`** を新規生成する（横・縦の2枚。下記「アセット」節・P-12 参照。2026-09-13 代表決定）。構図は、4人（霧島・橘・小鳥遊・新任）の対策室の間取りを決めたうえで、**画面には部屋の一部を切り取って映す**（見える席は奥の2席。奥の壁＝机2台と壁掛けモニタ／入口側の壁＝机2台〔画面に映らない〕／左の壁＝窓とブラインド／右の壁＝ホワイトボード／入口のそば＝紅茶のテーブル〔小鳥遊〕／奥の机に多肉植物〔霧島〕。2026-09-14 代表決定・#139）。**人物は描かない**（室長 日野は姿を見せない設定のため、日野の席自体を置かない。`docs/characters.md` §7）。生成は #139 で行い、プロンプトは下記「アセット」節・P-12 参照。**アセットIDはシナリオスキーマに持たせない**（導入は `scenes[]` を使わないため）。`intro-screen.tsx` 側の **UI 定数**として持つ方針は維持する（横・縦の切替も UI 定数側で行う。切替の決まりは下記「探索シーン」節「背景の箱」参照）。
  - **対策室の例外（2026-09-14 代表決定・#139）**: 導入③の対策室背景のみ、**昼の自然光の明るい部屋**にする。上記「単一のダーク文脈に統一」の配色ルールに対する**唯一の例外**とし、他の探索背景（S1〜SL、量産マップ）はサイバーパンク配色で作り直す。明るい背景の上でも、会話ウィンドウ・名前箱・中央選択パネルの文字が読めることは、上記「半透明（ガラス風）パネル」節の合成検証規則（純白・純黒の両方に対して4.5:1）で担保する（明るい背景は「純白寄り」のケースに相当するため、個別の実測をしなくても規則でカバーされる）。
  - **表情フォールバック**: 立ち絵は `PORTRAIT_SRC[character][expression]` で解決し、**該当 PNG が無ければ `neutral` にフォールバック**する。現在生成済みは霧島・橘・小鳥遊いずれも `neutral` のみ（下記「表情差分の定義表」参照）。これにより、シナリオデータには `expression`（`docs/scenario_schema.md` §2.6）を先に書き、絵は実使用分が固まった時点で追って生成できる（`IMAGE_WORKFLOW.md` の承認ゲートを経る）。
- **タイプライター表示（#52 Phase 4.7）**: 会話文は**1文字ずつ時間差で表示**する。タップ／Enter で即全文（スキップ）、`prefers-reduced-motion` では即全文。**選択肢・相談・カード閲覧などの操作要素は、全文表示（またはスキップ）後に出す**（送り途中の誤タップ防止）。支援技術には**全文を一度に渡す**（演出中テキストは `aria-hidden`、全文を visually-hidden で提供＝1文字ずつ読み上げさせない）。導入③・探索④・解決⑤の会話フレームで共通。
- **名前箱（2026-09-14 代表決定・#132。旧「名札」＝会話ウィンドウ左上のピルを置き換え）**: 話者名は、**いま話している人の立ち絵の下**に置く名前箱1つに表示する（サンプルの位置。会話ウィンドウ内の名札ピルは廃止）。**名前が出るのは1箇所だけ**という決まり（#119）はそのまま引き継ぐ。
  - **初期サイズ（秘書推奨・代表確認待ち #132。S1 試作で調整）**: **高さ＝背景の箱の高さの約5%・幅＝立ち絵カードの幅以下**。代表指定「サンプルの約半分」の初期値としてこの数値を置く。
  - **配置**: 立ち絵の下端に接し、その下に会話ウィンドウが続く（上記「立ち絵の拡大」節の積み順）。**名前箱は、いま話している側の枠にのみ表示する**（直前に話した側の枠には名前箱を表示しない。グレーアウトした立ち絵だけが残る）。これにより「名前が出るのは1箇所だけ」の決まりを常に満たす。
  - **見た目**: primary（ネオンブルー）背景＋background 色の文字（`--primary-foreground`）のピル。会話ウィンドウと同じ半透明パネルにはしない（常時判読できるよう不透明のまま）。
  - **NPC が話すとき（立ち絵なし・秘書推奨・代表確認待ち #132）**: **会話ウィンドウ上端の左**に、同じ見た目（primary 背景のピル）の名前箱を出す（未解決の質問3への回答）。左右2枠の立ち絵はどちらもグレーアウトしたまま動かさない（上記「左右2枠の入れ替わり方式」）。
  - **適用パート（秘書推奨・代表確認待ち #132）**: 導入③・探索④・解決⑤の**すべての会話フレームに適用**する共通コンポーネントとする（未解決の質問4への回答。共通コンポーネントのため部分ごとに挙動を変えない）。
  - 会話文は**明朝（世界観テキスト）**、名前箱・操作UIは**ゴシック**。
- **主人公の立ち絵は出さない**（主人公目線。`docs/characters.md` §3）。プレイヤー発話は会話文のみ、または選択肢で表す。
- **解決⑤の背景（代表決定 2026-09-13・#119）**: 解決パートは独自の背景画像を持たず、**「解決へ進む」を押した時点で表示していた探索シーンの背景をそのまま使う**（新しい画像は作らない）。導入③・探索④と同じ「背景の箱」＋立ち絵・会話ウィンドウの重ね配置を適用する（上記「探索シーン」節「背景の箱」参照）。実装は #124。
- **解決の会話モード（⑤）で背景の箱の中央に載せる選択パネル（2026-09-14 代表決定・#132）**:
  - **配置**: 背景の箱の**中央**に、コントロールパネル風の半透明パネル（上記「半透明（ガラス風）パネル」節）を置く。**問い＋選択肢＋相談ボタンはこのパネルの中**にまとめる（旧「選択肢は下部の会話ウィンドウ内」「相談ボタンは会話ウィンドウ内に配置」は撤回。#119 の記述を置き換える）。
  - **会話ウィンドウとの役割分担**: 下部の会話ウィンドウは**台詞（タイプライター）専用**にする。**会話ログは置かない**（代表が不採用）。誤答時の返答（「その場合だと〜」）も会話ウィンドウにタイプライターで表示する（選択パネルは開いたまま）。
  - **表示タイミング**: 上記「タイプライター表示」の原則どおり、**問いの全文表示（またはスキップ）後に選択パネルの選択肢・相談ボタンを操作可能にする**。全文表示前は選択パネル自体は表示してよいが、選択肢・相談ボタンは無効状態（下記5状態の「無効」）にする（送り途中の誤操作防止。既存の「操作要素は全文表示後に出す」原則を選択パネルにも適用）。
  - **カードドロワー**: 集めた手持ちカードをいつでも**無料で閲覧**できる引き出し（選択パネル表示中も開ける）。相談との違い（閲覧=無料／相談=回数消費）を UI で明示する。**箱の右上に「手持ちカード」ボタンを1つ置き、そこから開く**（探索④の「ヒント確認」ボタンと同じ位置・見た目・不透明表示。解決⑤は探索④のシーンタブ・調査ポイント一覧を持たないため、右上にはこのボタン1つだけになる）。中央の選択パネルとは重ねない。
  - **誤答フィードバック**: 選択肢は選択パネルに残したまま、相手の返答を会話ウィンドウに出し、再挑戦させる。外すたび解説が深まる（教育的失敗の統合）。

- **選択肢の5状態（2026-09-14 代表決定・#132。未解決の質問6=秘書推奨含む）**:

  | 状態 | 見た目 | 備考 |
  |---|---|---|
  | 通常 | surface-2 地に text-primary の文字、border（#6870AD）の枠 | |
  | ホバー | primary（ネオンブルー）のネオン光彩で縁取りを光らせる＋枠を primary に | キーボードのフォーカスと同じ見た目にする（下記） |
  | フォーカス | ホバーと同じネオン光彩＋**可視フォーカスリング**（2px solid primary、2px オフセット） | WCAG 2.4.7。ホバー用の光彩とフォーカスリングは別レイヤーとして重ねる（光彩だけでは非マウス操作者に伝わらないため） |
  | 押下（回答確定） | primary で塗りつぶし、文字は `--primary-foreground`（background色） | **ホバーの光彩とは別の状態**として区別する（未解決の質問6の前段。ホバー＝まだ選んでいない、押下＝回答を確定した瞬間の一時状態） |
  | 無効 | surface-2 地のまま彩度・不透明度を下げ、`disabled` 属性＋理由を `aria-label` で保持 | 全文表示前・相談0回時などに使用 |

  - `prefers-reduced-motion: reduce` では、ホバー・フォーカスの**光彩アニメーションを止める**（枠色の変化のみ残す。静止した強調表示は維持し、情報が消えないようにする）。
  - 各ボタン 48px 以上・素の `<button>`。Tab/Enter/Space で完遂できる（旧カード配置の dnd-kit は廃止＝選択式のほうが WCAG 的に素直）。

- **相談ボタン（2026-09-14 代表決定・#132。未解決の質問7=#135 へ引き継ぎ）**:
  - 選択パネル内に配置し、**残り回数をカウンタアイコン（数字付き）で表示**する（マップ3回）。
  - **XP が減ることを警告アイコンで示す**（色だけに頼らず、`aria-label` に「相談する・残り n 回・使うと XP が {`CONSULT_XP_PENALTY`=15} 減る」相当のテキストを持たせる。WCAG 1.4.1）。
  - 0 回のときは無効表示＋理由を保持する（上記5状態の「無効」）。
  - アイコンは `lucide-react`（既存依存・SVG 自作の承認ゲート不要）を使う（#135 未解決の質問2への回答）。#30（UI アイコン工程の不在）は、カード種別アイコン等の SVG 自作対象とは切り離し、**lucide-react で足りるアイコン（相談・XP警告・時刻等の操作UIアイコン）は #30 の対象外**として整理する（カード種別7種・正誤フィードバックアイコンなど独自デザインが要るものだけ #30 で SVG 自作を検討する）。

- **XP バー（2026-09-14 代表決定・#132。未解決の質問7=#135 の質問1への回答）**:
  - 解決中、**「このままクリアした場合の獲得 XP 見込み」**をバーと数値で表示する（秘書推奨。累計 XP ではなく、100 からの減りを見せる案）。
  - 誤答・相談のたびに更新する（計算は `save-integration.ts` の `CLEAR_XP_REWARD`(100) − 誤答×`WRONG_ANSWER_XP_PENALTY`(10) − 相談×`CONSULT_XP_PENALTY`(15)、下限0、と同じ純粋関数を共有し、クリア時の XP と必ず一致させる。実装は #135）。
  - 数値をテキストでも表示し、支援技術にも伝える（バーの視覚表現だけに頼らない）。
  - 表示位置は下記「縦長（9:16）の構成」節の表のとおり。

## ゲーム内時刻

- **表示位置**: 背景の箱の**右下**に、他要素と重ならない位置で表示する（横長・縦長とも。秘書推奨・代表確認待ち #132。サンプルは右下のタイムスタンプ表示）。
  - **ただし探索④は例外**: 右下は「解決へ進む」ボタン（`canEnterResolution` 成立中は常に活性で右下に重ねて表示される。上記「探索完了→解決への誘導」節）と衝突するため、探索④のみ**会話ウィンドウ帯の右上端**（会話ウィンドウが出ていないとき＝探索状態では、シーンタブの行と重ならないよう**シーンタブの下**に表示する）に表示する。導入③・解決⑤は「解決へ進む」ボタンを持たないため右下のままでよい。
- **粒度・データ**: `HH:MM` 形式の文字列をシナリオスキーマに持たせる（#136 で定義）。表示先は導入・探索の各シーン・解決にそれぞれ1つ（#136 秘書推奨に合わせる）。
- **アイコン**: `lucide-react` の時計アイコン＋テキストを併用する。

## 縦長（9:16）の構成

背景の箱に対する**相対位置（%）**で、横長（16:9）・縦長（9:16）双方の主要要素の配置を定義する（#124 の「縦長の画面は常に 9:16 の箱」「縦背景なしは横画像を上部に表示」は変えない。縦スクロールを出さない原則も維持）。数値は S1 試作（#134/#135/#138）で微調整する前提の目安値。

| 要素 | 横長（16:9） | 縦長（9:16） |
|---|---|---|
| 右上ボタン群（探索④＝ヒント確認／調査ポイント一覧。解決⑤＝手持ちカード1個のみ） | 箱上端に接し右寄せ（従来どおり） | 同左（幅が狭い分ボタンは縦積みでもよい） |
| シーンタブ（探索④のみ） | 右上ボタン群の下 | 同左 |
| 立ち絵（左右2枠） | 箱の左右の端、高さは箱の45〜50% | 箱の左右の端、高さは箱の30〜35%（上記「立ち絵の拡大」節） |
| 名前箱 | 各立ち絵の直下、高さ＝箱の高さの約5%・幅≦立ち絵幅 | 同左（比率は横長と共通） |
| 中央の選択パネル（解決⑤のみ） | 箱の**水平中央**、左右の立ち絵の間（幅は箱の50〜55%程度） | 立ち絵2枠の**上**（箱の上部〜中段）に配置し、立ち絵は選択パネルの下・会話ウィンドウの上に収める（横長のように立ち絵の間に挟む横幅の余裕がないため。上から: 右上の「手持ちカード」ボタン→選択パネル→立ち絵2枠＋名前箱→会話ウィンドウ、の縦積み） |
| 会話ウィンドウ | 箱の下部、幅いっぱい | 箱の下部、幅いっぱい |
| XPバー（解決⑤のみ） | 選択パネルの上辺 | 選択パネルの上辺（縦積みの一番上に近い位置） |
| ゲーム内時刻 | 箱の右下（探索④は会話ウィンドウ帯の右上端） | 同左 |

- いずれの要素も背景の箱の外にはみ出さず、箱の下に並べたりしない（上記「重ねる要素は背景の箱に対する相対位置」の原則を踏襲）。

## 探索シーン（背景＋クリック可能オブジェクト・#52 で導入）

探索④は、事件現場の**背景シーン**の上に**クリック可能オブジェクト（ホットスポット）**を置き、タップして調べ、結果を会話フレームで受け取る。背景シーンのコンセプトは背景モックで代表確認済み（2026-09-10）、会話フレーム化・不可視ホットスポットは T018'' 代表注釈（2026-09-11）で確定（いずれも Obsidian タスクに保持・**Public リポジトリに URL を載せない**）。

- **背景の箱（#119、縦長の画面は常に9:16に改訂・代表決定2026-09-14・#124。#125の記述を改める）**: 画面いっぱい（100dvw×100dvh）に収まる**最大の 16:9（横長の画面）または 9:16（縦長の画面）の矩形**を画面中央に置く。横長・縦長の判定は**画面幅ではなく画面の向き（`orientation`）**で行う（タブレットを横に持ったときは 16:9）。**縦長の画面は、縦の背景アセットの有無に関わらず常に 9:16 の箱**にする（旧「縦の背景が無いシーンは縦長の画面でも16:9のまま」は撤回）。**縦の背景アセットが無いシーン**では、9:16 の箱の**上部**に横長画像を**幅いっぱいで表示**し（`contain`・上寄せ、高さ＝幅×9/16）、残りは**背景色のトークン**（`background`）で塗る。**ホットスポットは横の画像の描画矩形に対する `landscape` 座標で置く**（箱ではなく画像の矩形が基準。縦の背景があるシーンは従来どおり箱全体＝画像で `portrait` 座標）。矩形の外にできる余白（横長の箱の左右レターボックス等）も同じく背景色のトークンで塗る。**旧「16:9 を既定・モバイル縦はレターボックス＋横パン」は撤回**（横パンは使わない）。横回転は強制しない。実装は `src/ui/lib/background-box.ts` の `resolveBoxOrientation`/`resolveBackgroundImageRect`/`resolveHotspotBoxPosition`（純粋関数・単体テストあり）。
- **重ねる要素は背景の箱に対する相対位置（#119）**: ホットスポット・立ち絵・会話ウィンドウ・右上のボタン群（ヒント確認・調査ポイント一覧）は、いずれも**背景の箱に対する相対位置**で置く。箱の外にはみ出したり、箱の下に並べたりしない。**縦スクロールを出さない**（下記「会話オーバーレイのレイアウト」節も同じ原則）。
- **縦長・縦の背景アセットが無いシーンでの横画像の開始位置（秘書レビュー2回目・代表決定2026-09-14・#124）**: 探索④はシーンタブ・右上のボタン群（ヒント確認・調査ポイント一覧）を箱の**上部**に重ねるため、横画像を箱の最上部から表示すると画像・ホットスポットの一部がこれらのボタン群/タブの下に隠れてしまう。そのため探索④に限り、**横画像（とホットスポット）の開始位置（top）をボタン群・シーンタブの行の高さぶん下げて**から表示する（`resolveBackgroundImageRect`/`resolveHotspotBoxPosition`の`topOffset`引数、実装は`src/ui/components/explore/scene-explorer.tsx`）。ボタン群・シーンタブ自体は従来どおり箱の最上部（`top-2`/`top-16`）のまま。導入③・解決⑤（シーンタブ・右上ボタン群を持たない画面）はこのオフセットを使わない（従来どおり箱の最上部から表示）。
- **背景**: 立ち絵と同じアニメ調で image_agent 自作（IMAGE_WORKFLOW）。**場所ごとに複数背景**（S1 は執務室／サーバ室の2シーン）を**シーンタブ**で切替（切替もキーボード到達可能）。**縦の背景がまだ無いマップは、縦長の画面では 9:16 の箱の上部に横画像を幅いっぱいで表示**する（代表決定2026-09-14・#124。上記「背景の箱」参照。4マップすべてに縦が揃うまでの暫定運用。4マップ揃った時点で縦の背景を必須化する。下記「アセット」節参照）。
- **シーン移動＝ドア（#52 Phase 4.7・T018'''' 反映）**: シーンタブに加え、**背景内のドア（`object_type: door`）をタップして別シーンへ移動**できる（`goto` アクション・移動先シーンID を指定）。タブとドアの**両方**で移動でき、動線を分かりやすくする。ドアも通常不可視・ホバー/フォーカスで□マーカー・`aria-label`（例:「執務室への扉」）を保持。背景にドアが描かれていないシーンは、通路の奥・出入口相当の位置に置く（背景の再生成はしない）。
- **ホットスポット（#52 Phase 4.7 で表現変更）**: **通常はアイコンも名前ラベルも表示しない**（背景の絵に溶け込ませる）。**カーソルホバー／キーボードフォーカス時に□マーカー**（矩形ハイライト）で位置と操作可能を示す。**□マーカーの枠線は赤系**（視認性のため。T018''' 代表フィードバック 2026-09-11。トークン化して用いる＝カラーコード直書きしない。既存の危険操作トークン warning とは別に、探索ホットスポットの強調色として扱う）。実体は**実 `<button>`（48px 以上）**で、`aria-label` に種別・名前・調査済みか（例:「経理部PC・調査済み」）を常に保持し、**フォーカスは可視**（WCAG 2.4.7）。□マーカーはフォーカス可視も兼ねる。※ 旧仕様（色＋アイコン＋ラベルを常時表示）は T018'' 代表フィードバックで廃止。
- **調査結果の提示＝会話フレーム（#52 Phase 4.7）**: 人物の証言だけでなく、**PC のログ・書籍の文献も含めて、調査結果はサポート役の台詞として会話フレームで提示**する（上の「会話フレーム」節・タイプライター共通）。話者は調査3系統から既定を導出（ログ→霧島／証言→橘／文献→橘。技術文献は `speaker` 明示で霧島）。台詞は `collect` アクションの省略可能な `line`/`speaker`（`scenario_schema` §2.5）を使い、省略時は既定の導入文＋カード本文にフォールバック。**多ターンの問いかけ（#100）は `collect.dialogue[]`（`scenario_schema` §2.6）で表現し、`line`/`speaker` 単発とは併用しない。**
- **NPC 直接発話の描画（#100・`collect.dialogue` 限定）**: 会話行が `npc`（NPC名の自由記述。例:「中野」「経理部長」「サーバ管理者」）を持つ場合、**霧島・橘の両立ち絵をグレーアウト**し、**会話ウィンドウ上端の左の名前箱に `npc` の値をそのまま表示**する（上記「会話フレーム」節「名前箱」の NPC 発話時の位置・スタイル）。あわせて、**トリガー元のホットスポットを□で強調**する（`collect` は必ずホットスポット起点のため、UI側で対象ホットスポットを特定できる。データ追加は不要）。NPC 発話は探索の `collect.dialogue[]` に限られ、導入・解決の会話フレームには出現しない（小鳥遊ガードと同様、使える箇所が限定される。`docs/scenario_schema.md` §2.6）。
- **2つの状態（#52・T018''''' 代表モック確定 2026-09-11）**: 探索画面は「探索状態」と「会話状態」を切り替える。
  - **探索状態（既定）**: 背景シーン＋ホットスポット（と右上「調査ポイント」ボタン・シーンタブ）**のみ**を表示する。**立ち絵も会話ウィンドウも表示しない**（カーソルでのホットスポット探索を邪魔しないため＝代表指定）。
  - **会話状態（調査結果／証言／danger の教育的FB を見せる間）**: 背景シーンを暗転させず保持したまま、その上に会話 UI を**重ねる**。**閉じる**と探索状態に戻り、立ち絵と会話ウィンドウは消える。
- **会話オーバーレイのレイアウト（会話状態のとき）**: ノベルゲーム的な没入配置（従来の「背景の下に立ち絵バンド＋ウィンドウを積む」形は廃止）。重ねる要素:
  - **左右端に立ち絵（左右2枠の入れ替わり方式）**（上の「会話フレーム」節「左右2枠の入れ替わり方式」と共通のロジック。#108。旧「霧島＝左・橘＝右で固定」は廃止）。**背景の箱の左右の端**に置き、右上のボタン群とは重ねない（#119）。**立ち絵の下端に名前箱、その下に会話ウィンドウという積み順**にする（上記「会話フレーム」節「立ち絵の拡大」参照。旧「会話ウィンドウの上端に接する」は #132 で撤回済み）。ダーク枠のカードで画面端に置き、背景中央の事件現場と重ならないようにする。**いま話している人の枠はフルカラー・もう一方の枠はグレーアウト**（会話フレーム節「発話者の強調」と共通）。話者の枠の色は**白**（代表決定 2026-09-14。会話フレーム節「話者の枠」参照）。
  - **画面下部に会話ウィンドウ**（半透明パネルの帯）。台詞（タイプライター）＋「〜を調べた結果」の文脈行（話者名は会話ウィンドウ内には出さず、上記「会話フレーム」節「名前箱」のとおり立ち絵の下・NPC 発話時は会話ウィンドウ上端の左に表示）。**「閉じる」ボタンは置かない＝会話中に画面をクリック/タップで閉じる**（T018'''''' 代表指定。タイプライター送出中のクリックはスキップ＝全文表示、全文表示後のクリックで会話状態を閉じ探索状態へ戻る）。カード確認・調査ポイントは下記の右上ボタン群に集約（ウィンドウ内には置かない）。
  - **右上のボタン群（探索状態・会話状態とも常時表示）**: 右に**「調査ポイント一覧」**トグル、その**左に「ヒント確認」**（＝手持ちカード閲覧＝`card-drawer`・無料。旧 ? アイコンを廃し文言ボタンに）。**両ボタンとも透過させず、背景（surface＋border）で「そこにボタンがある」と分かる不透明表示**にする（T018'''''' 代表指定。**上記「半透明（ガラス風）パネル」節の適用対象外の例外として維持**＝発見性優先。#132）。`aria-label` を保持。
  - モバイル（狭幅・縦長の画面）でも同じ重ね配置。立ち絵は**背景の箱（9:16）の高さに対する比率**で表示し（会話フレーム節「立ち絵の拡大」参照）、ウィンドウ幅は画面いっぱい。タップ／Enter で送出・スキップは会話フレーム共通。
  - キーボード到達順は「ヒント確認 → 調査ポイント一覧（トグル） → 背景のホットスポット → 会話状態では会話ウィンドウ（1つの操作領域。スキップ/閉じる）」を崩さない（T048。旧「閉じる・?・進行」は右上ボタン群への統合とクリック閉じ化により解消）。
- **PC 操作メニュー**: PC/機器タップで「ログを取る／電源を落とす／今は触らない」等のアクションシート。**「電源を落とす」は教育的フィードバック（揮発性メモリ喪失・橘の台詞・会話フレーム）**を出すが、**探索ではペナルティにしない**（電源後もPCは操作可＝詰み防止・XP減算なし。spec §8.4）。
- **アクションシートの見出し＝挨拶台詞（#52 Phase 4.7・T018'''' 反映）**: ホットスポットに省略可能な `prompt`（例: サーバ管理者「どうしましたか？」）を持たせ、アクションシートの見出しに出す。省略時はラベルのみ。
- **アクションシートの表示位置・半透明化・「戻る」選択肢の必須化（#52 追補・代表FB 2026-09-11）**: アクションシートは旧・画面下部/ホットスポット近傍のパネル表示ではなく、**見出し（prompt/ラベル）＋2〜3個の選択肢ボタンを背景シーンの箱の中央にオーバーレイ表示**する（会話オーバーレイと同じ絶対配置のコンテナに重ねる）。**選択肢ボタンは上記「半透明（ガラス風）パネル」節の最小不透明度トークン（`--glass-panel-min-alpha`=0.81）に従う半透明**にし、背景シーンがうっすら透けて見えるようにする（旧「不透明度80%程度・`bg-card/80`」は本節のトークンに統一）。右上の「ヒント確認」「調査ポイント一覧」は発見性のため従来どおり不透明のまま（別要件・半透明化の対象外）。**「何もせず戻る」選択肢を必ず用意**する: データに `noop`（例「今は触らない」「何でもない」）が無いアクションシートには、UI側で「閉じる（何もしない）」を選択肢の末尾に補う（既存の並び順は変えず、既に `noop` がある場合は二重に足さない）。選択肢は縦積み・各48px以上・Tab/Shift+Tab＋Enter/Spaceで完遂でき、**Escapeは常にアクションシートを閉じる**（会話オーバーレイのEscape挙動と統一）。
- **系統をまたぐ統合ホットスポット（#52 Phase 4.7・T018'''' 反映）**: 物理的に同じ場所の人と機器は1つのホットスポットに束ねる（例: サーバ室の「サーバ管理者」＝人物＋その運用端末。`prompt`「どうしましたか？」→「話を聞く（証言・橘）／PCを確認する（ログ・霧島）／何でもない（noop）」）。ホットスポットの被り（人物とPCが重なって場所が分かりにくい）を避ける狙い。話者は各 `collect` の `speaker`／3系統既定で決まる。
- **カード閲覧（「ヒント確認」ボタン）**: 右上ボタン群の**「調査ポイント一覧」の左**に**「ヒント確認」**（＝手持ちカード閲覧＝`card-drawer`・無料・`aria-label="手持ちカードを見る（無料）"`）を常時置く（探索状態・会話状態とも）。**会話ウィンドウ内には置かない**（T018'''''' で右上へ移動）。解決の `card-drawer` と同じ（無料閲覧・相談の回数消費とは別物）。
- **一覧フォールバック（必須）**: ホットスポットが不可視のため、**右上の「調査ポイント一覧」ボタンは探索状態・会話状態とも常に表示**し（不透明で発見性を担保）、押すと一覧を開く。一覧から**背景に頼らずキーボードのみで全ポイント調査→解決へ**進めること（WCAG）。トグル常時可視のため、旧「モバイルでは一覧を初期表示」は本トグル方式に置き換える（探索状態では立ち絵・ウィンドウを出さずカーソル探索を優先＝代表指定）。
- **進捗**: 収集済み手がかり数と「解決へ」の活性条件（必要な手がかりが揃うと活性）を示す。
- **探索完了→解決への誘導（#52 Phase 4.7・T018''' 反映、追補で解決画面遷移化、PR#92追補で「閉じて再探索も可・ロックしない」形に改訂）**: 必要な手がかりが揃った（＝「解決へ」の活性条件を満たした）時点で、**霧島または橘が会話フレームで「そろそろ問題をまとめようか」と解決フェーズへ促す**（プレイヤーが次に何をすべきか迷わないように）。促しの台詞は会話フレーム（タイプライター）で1回出す。
  - **「わかった」＝解決へ**: ボタン文言は「わかった」のままだが、押すと探索状態には戻らず、**下の「解決へ進む」ボタンと同じ遷移（`canEnterResolution` 成立時の解決画面への遷移）でそのまま解決画面へ進む**。
  - **画面（会話ウィンドウの外側）をクリック/タップ、または Escape で閉じて探索を継続できる**（PR#92追補・代表FB「ロックしない」）。誘導会話が出ている間は他の会話オーバーレイと同様に背景のホットスポットを一時的に隠すため、閉じる手段が無いと手がかりが揃った後は「わかった」で解決へ進むことしかできなくなってしまう（背景ホットスポットの再探索がロックされる）。会話ウィンドウ**自体**やその中の「わかった」ボタンのクリックでは閉じない（ボタンの入れ子を避けるため、専用の `onOutsideDismiss` を使う。閉じるとホットスポットが再び操作できる探索状態に戻る）。
  - **一度閉じたら自動的には再表示しない**（ナグ防止）。
  - **「解決へ進む」ボタンは誘導会話の表示・非表示に関わらず、`canEnterResolution` 成立中は常に活性のまま使える**（誘導会話を閉じた後もそのまま解決へ進める＝どちらの経路からでもロックしない）。ただし**自身の会話（調査結果／danger の教育的FB）が開いている間だけは非表示にする**（秘書レビュー2回目・代表決定2026-09-14・#124: 会話ウィンドウが箱の下部いっぱいに広がるため、同じ箱右下に重ねる「解決へ進む」と衝突する不具合の修正）。自身の会話にはクリック/タップ/Escapeで閉じる手段があるため、閉じて探索状態に戻ってから改めて「解決へ進む」を押せる（迷子にしない）。誘導会話（わかった）は従来どおり表示し続ける（上記「誘導会話の表示・非表示に関わらず」の対象は誘導会話のみで、自身の会話とは扱いが異なる）。
  - 誘導会話はクリックで閉じる他の会話オーバーレイ（調査結果・danger）とは閉じ方が非対称（ウィンドウ外側 vs ウィンドウ自体）だが、「わかった」という実ボタンを持つため（ボタンの入れ子を避ける設計上の理由）であり、代表了承済み。
  - 話者は分野の主導役（当面は橘＝司令塔を既定、`docs/characters.md`）。
- **話者の割り当て（#56 実装メモ→#52 Phase 4.7 で更新）**: #56 では core スキーマに話者フィールドが無く、`SceneExplorer` は証言表示の話者を橘に固定していた（暫定）。また証言カードの選び方が種別を問わず「非ダミー先頭」だったため、対策カード等が証言として表示される不具合があった（#62）。**Phase 4.7 で `collect` アクションに省略可能な `line`/`speaker` を追加**（`scenario_schema` §2.5・schema 0.5.0）し、話者は明示 or 3系統からの既定（ログ→霧島／証言→橘／文献→橘）で決める。これにより「非ダミー先頭を証言として読む」経路自体が無くなり、**#62 は Phase 4.7（T044）で吸収**する。
- **danger（教育的フィードバック）の配色（#56 実装メモ・2026-09-14 #132 でトークン値を更新）**: 上表の `warning` トークンはまだ
  CSS変数として配線されていない（`src/index.css` は現状 shadcn 既定のグレースケールトークンの
  みで、DESIGN.md の warning/success/info の hex 値は未実装。**トークン配線は #133（T062-ui）を
  俟つ**。旧「T012 系のタスクを俟つ」は Phase 4.11 化に伴い更新）。そのため探索の危険操作フィードバックは ⑤解決の誤答フィードバック（`resolve-screen.tsx`）
  と同じ中立トーン（`border-border`/`bg-background`）で暫定表示する。warning トークンが配線され
  次第、両画面とも `warning` トークンへ差し替える。

## インタラクション・アクセシビリティ

- タップ/クリック対象 最低 48×48px（モバイル）／32×32px（デスクトップ）
- インタラクティブ要素は hover / focus / active / disabled を持つ
- 操作には 200ms 以内にフィードバック
- 解決パート（会話モード・#42）は**選択肢ボタンで回答**する。**キーボードのみでも**問いへの回答・相談・カード閲覧・クリアまで完遂できること（旧カード配置のドラッグ操作は廃止）
- **色だけで情報を伝えない**：正誤・カード種別（証言／ログ／通信記録／外部情報／暗号文／鍵／対策）は**色＋アイコン＋ラベル**で区別
- 画像に代替テキスト、フォーム入力にラベル
- IPA 過去問由来素材の**出典表記**を結果画面等に明示（spec FR-7）

## アセット

画像アセットの単一の参照先。追加・生成のたびにここを更新する（正本: `IMAGE_WORKFLOW.md`。承認ゲートを経ずに生成しない）。
キャラクターの設定側の正本は `docs/characters.md`（外見を変えるときは両方を同時に更新する）。

**サイバーパンク改訂（2026-09-14・#132）に伴う扱い**: 背景12枚（横8・縦4）は**CSSで色味を補正せず作り直す**（代表決定・背景参照）。探索の背景（S1・S2・S3・SL、量産マップ含む）は新しい配色で作る。**対策室（導入③）の背景のみ、昼の自然光の明るい部屋のまま**にする例外（上記「会話フレーム」節「対策室の例外」参照・#139）。作り直しは #139〜#143 で行い、本 Issue（#132）ではプロンプト本文は書き換えない。**下表・下記プロンプト本文は再生成までの過去の生成記録としてそのまま残す**（プロンプトの「金色」「生成り色」「暖色寄りの暗い焦げ茶」「静かで硬派な推理アドベンチャーの雰囲気」等の色・トーン表現は、生成時点の指定を保存する記録であり、上記の新カラートークンに書き換えない）。新配色での色名の翻訳は、**#139〜#143 のプロンプト作成時**に以下の対応例に沿って行う（本 Issue では方針のみ）:

| 旧トークン日本語表現 | 新トークン日本語表現（例） |
|---|---|
| 暖色寄りの暗い焦げ茶（background/surface） | 深い藍紫（ネイビー×パープルの暗色） |
| 生成り色（text-primary のハイライト） | 淡い水色がかった白 |
| 金色をごく少量（primary） | 発光するようなネオンブルーの光の差し色 |
| （新規）状態色の差し色 | ネオングリーンの光の差し色（ごく少量。状態・成功の演出に限定） |

**立ち絵の扱い**: 立ち絵は描き直さない（代表が不採用）。無地の焦げ茶背景の立ち絵カードが新しい配色から浮く場合は、**CSS でカードの枠・色調を合わせる**（上記「対策室の例外」節の合成検証と同じ考え方で、立ち絵カードに `border` トークンの枠線と、`surface` トークンを薄く重ねたオーバーレイ（例: `surface` を 10〜15% 程度のブレンドで乗せる）を掛け、画面のダーク基調になじませる。立ち絵の絵そのものは変更しない）。

| アセットID | 用途 | 使用画面 | 寸法/アスペクト比 | アートスタイル | 生成手段 | 生成済みパス | 生成プロンプト |
|-----------|------|---------|-----------------|--------------|---------|------------|--------------|
| char-kirishima-neutral | 霧島 悠 立ち絵（通常） | ③導入／④探索／⑤解決／⑦結果 の会話フレーム | 3:4 / 864×1184 px（切り抜き前・腰上） | アニメ調・細線・静かで硬派な推理劇のトーン | image_agent（gemini-2.5-flash-image） | `assets/characters/kirishima-neutral.png` | 下記 P-1 |
| char-tachibana-neutral | 橘 澪 立ち絵（通常） | 同上 | 3:4 / 864×1184 px（切り抜き前・太もも上） | 同上 | image_agent（gemini-2.5-flash-image） | `assets/characters/tachibana-neutral.png` | 下記 P-2 |
| char-takanashi-neutral | 小鳥遊 綾香 立ち絵（通常） | 同上 | 3:4 / 864×1184 px（切り抜き前・腰上） | 同上 | image_agent（gemini-2.5-flash-image） | `assets/characters/takanashi-neutral.png` | 下記 P-11 |
| char-player | 主人公 立ち絵 | — | — | — | **作らない** | — | —（画面は主人公目線で構成するため不要。2026-09-09 代表決定） |
| bg-s1-office | S1 探索背景（執務室） | ④探索 | 16:9（1344×768 px） | アニメ調・細線・立ち絵と統一のトーン | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-s1-office.png` | 下記 P-3 |
| bg-s1-server | S1 探索背景（サーバ室） | ④探索 | 同上 | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-s1-server.png` | 下記 P-4 |
| bg-s2-office | S2 探索背景（執務室・情シス部門） | ④探索 | 16:9 | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-s2-office.png` | 下記 P-5 |
| bg-s2-server | S2 探索背景（サーバ室・保守業者） | ④探索 | 同上 | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-s2-server.png` | 下記 P-6 |
| bg-s3-office | S3 探索背景（EC事業者の執務室） | ④探索 | 16:9 | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-s3-office.png` | 下記 P-7 |
| bg-s3-ops-room | S3 探索背景（システム運用ルーム） | ④探索 | 同上 | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-s3-ops-room.png` | 下記 P-8 |
| bg-sl-office | SL 探索背景（法務・自社執務室） | ④探索（旧: ③導入(S1) にも流用していたが #119 で撤回。導入の背景は `bg-hq-taskforce` に置き換え） | 16:9 | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-sl-office.png` | 下記 P-9 |
| bg-sl-vendor | SL 探索背景（法務・委託先ブース） | ④探索 | 同上 | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-sl-vendor.png` | 下記 P-10 |
| bg-hq-taskforce | 対策室背景（横／縦。#139 で作り直し：窓・壁のモニタ群・ホワイトボードのある部屋、昼の自然光＝配色統一の例外） | ③導入 | 16:9（1344×768 px） | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-hq-taskforce.png` | 下記 P-12（横） |
| bg-hq-taskforce-portrait | 対策室背景（横／縦。#139 で作り直し：窓・壁のモニタ群・ホワイトボードのある部屋、昼の自然光＝配色統一の例外） | ③導入（縦長の画面） | 9:16（768×1344 px） | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-hq-taskforce-portrait.png` | 下記 P-12（縦） |
| bg-s1-office-portrait | S1 探索背景（執務室・縦） | ④探索（縦長の画面） | 9:16（768×1344 px） | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-s1-office-portrait.png` | 下記 P-13 |
| bg-s1-server-portrait | S1 探索背景（サーバ室・縦） | ④探索（縦長の画面） | 9:16（768×1344 px） | 同上 | image_agent（gemini-2.5-flash-image） | `assets/backgrounds/bg-s1-server-portrait.png` | 下記 P-14 |

**未定（Issue #30 で判断・工程化する）**: カード種別アイコン7種（証言／ログ／通信記録／外部情報／暗号文／鍵／対策。WCAG 1.4.1 由来で必須・SVG 予定）、正誤フィードバックアイコン（SVG 予定）、アプリアイコン／favicon（PWA 各サイズ）、タイトル画面キービジュアル。

**縦の背景（9:16）の未着手分（#119）**: S2/S3/SL の縦背景（`bg-s2-office`/`bg-s2-server`/`bg-s3-office`/`bg-s3-ops-room`/`bg-sl-office`/`bg-sl-vendor` それぞれの `-portrait`）は**未作成（NPC の外見の確定後）**。S1・導入（対策室）の縦は上表のとおり #121・#122 で先行着手する。

**探索背景（#52 で工程化・#30 から前倒し）**: 探索画面の背景シーンは image_agent 自作（**横16:9**・アニメ調で立ち絵と統一。縦9:16は #119 で追加した「背景の箱」の切替方式に対応するため、#121 以降順次生成）。MVP は S1 の2枚（執務室／サーバ室＝上表 bg-s1-*）。**2026-09-10 に IMAGE_WORKFLOW の承認ゲート（アセット定義＋プロンプト提示→代表承認）を通し、各2案生成→代表が各1案を採用して確定**（T039）。検索で見つけた画像は構図・雰囲気の参考に留め、ライセンス・画風統一の観点から流用していない（image_agent 自作）。量産マップは各2〜3枚を各制作 Issue に含める。生成元 Drive URL・不採用案は Obsidian タスク `Tasks/po_agent/crypto-riddle/20260910_explore_background_scene.md` に記録。

**縦の背景のプロンプトの原則（#119）**: 今後、縦（9:16）の背景を生成する際は、**上端（右上のボタン群が重なる領域）と下 1/3（会話ウィンドウが重なる領域）を空け**、人物・機器は**中段に集める**構図にする（#121・#122 のプロンプト作成時に適用。プロンプト本文は各 Issue の承認ゲートで代表確認後に追記する）。「上端・下端を余白にする」とだけ書くと、モデルが白い帯として描くことがある。上下に描くもの（天井の照明・手前の床など）を具体的に書き、「画面の上下左右の端まで室内の絵で埋め、白い余白・枠・帯は入れない」と明記する（#121・#122 の生成で確認）。

立ち絵の運用メモ（2026-09-09 確定）:

- 切り抜いて使う前提のため、生成時の背景色は問わない（単色の無地で生成。透過 PNG は未作成で、UI 実装時に切り抜く）
- 表情差分は「表情差分の定義表」（Issue #97）参照。候補C（取り乱さない設計）のため「通常（neutral）」1種で開始し、必要になった時点で本表（アセット表）に行を追加する
- プロンプトの色指定はダーク文脈トークンを日本語に翻訳したもの（background/surface → 暖色寄りの暗い焦げ茶、text-primary → 生成り色、primary → 金色）
- 生成元の Drive URL・不採用案とその理由は Obsidian タスク `Tasks/po_agent/crypto-riddle/20260909_character_portraits.md` に記録

**表情差分の定義表（Issue #97・定義のみ）**

サポート役3名（霧島・橘・小鳥遊）の表情差分を `neutral / serious / confident / smile / thinking` の5種で定義する。**本表は定義のみであり、neutral 以外は未生成**。実生成は S1 台本v2承認後に実使用分だけ行う（承認ゲートを経ずに生成しない。`IMAGE_WORKFLOW.md`）。生成済みアセットの正本は上のアセット表（`char-kirishima-neutral` / `char-tachibana-neutral` / `char-takanashi-neutral`）。

| キャラクター | neutral | serious | confident | smile | thinking |
|---|---|---|---|---|---|
| 霧島 悠 | 生成済み（`char-kirishima-neutral`） | 未生成 | 未生成 | 未生成 | 未生成 |
| 橘 澪 | 生成済み（`char-tachibana-neutral`） | 未生成 | 未生成 | 未生成 | 未生成 |
| 小鳥遊 綾香 | 生成済み（`char-takanashi-neutral`） | 未生成 | 未生成 | 未生成 | 未生成 |

**P-1（霧島 悠・採用＝第2案）**

> 30代前半の日本人男性のセキュリティ技術者を腰上で正面やや斜めから描いた立ち絵。寡黙で冷静な実力者という人物像で、表情は硬く引き締まり笑みはなく、眼鏡はかけず、視線はまっすぐ前を見ている。服装はダークグレーのジャケットに、インナーは白いワイシャツでネクタイはなく第一ボタンを一つ外している。中堅システム会社のオフィスに勤める技術者らしい控えめな装いで、装飾品やピンは付けない。色は暖色寄りの暗い焦げ茶の無地背景に、生成り色のハイライトと、金色をごく少量だけ差し色として使う。派手さを抑えた静かで硬派な推理アドベンチャーの雰囲気のアニメ調イラストで、線は細く清潔、背景は無地で切り抜きやすくする。

**P-2（橘 澪・採用＝第3案）**

> 30代前半の日本人女性の法務担当者を頭から太ももまで正面やや斜めから描いた立ち絵。明晰で場を動かす司令塔という人物像で、目元は切れ長で鋭く、表情は落ち着いて明快、口元にわずかな余裕のある笑みがあり、口元の横に小さなほくろが一つある。視線はまっすぐ前を見ている。髪は肩を越える長さのストレートのロングヘアで下ろしており、細いフレームの眼鏡をかけている。胸が大きく豊かな体型で、服装は深いワインレッドのテーラードジャケットと同色のタイトなミニスカート、インナーは黒のブラウス。装飾品やピンは付けない。背景は枠や粒子や模様が一切ない完全な単色の無地で、切り抜きやすくする。派手さを抑えた静かで硬派な推理アドベンチャーの雰囲気のアニメ調イラストで、線は細く清潔。

**P-11（小鳥遊 綾香・採用＝丸眼鏡版 A案。反復5回で確定）**

> 20代半ばの日本人女性の庶務・受付担当を腰上で正面やや斜めから描いた立ち絵。橘や霧島より背が低く小柄で華奢な体格、頭身は低めで幼く見える。おっとりとした和み系の人物像で、柔らかな下がり眉とやさしい笑顔。牛乳瓶の底のように非常に分厚い丸縁の眼鏡（ぐるぐる眼鏡・瓶底眼鏡）をかけ、白く不透明に光るレンズで両目とその周りの顔の輪郭が完全に覆い隠され、瞳はまったく見えない。眉から下の目元は分厚いレンズに隠れている。髪はふんわりとした肩下までの明るい茶髪をハーフアップにまとめている。服装はライトベージュのカーディガンに白いブラウス、膝丈のフレアスカート。髪飾りは付けない。色は暖色寄りの暗い焦げ茶の無地背景に、生成り色のハイライトと金色をごく少量だけ差し色として使う。派手さを抑えた静かで硬派な推理アドベンチャーの雰囲気のアニメ調イラストで、線は細く清潔、背景は無地で切り抜きやすくする。（`--aspect-ratio 3:4`）

探索背景の運用メモ（2026-09-10 確定・T039）:

- 立ち絵と違い**切り抜かず全画面背景として使う**。UI 側はこの上にホットスポット（実 button）を相対座標で重ねる（`scene-explorer.tsx`）。ホットスポットを重ねる余白を残す構図で生成した。
- 各2案生成し代表が各1案を採用（執務室・サーバ室とも第2案）。生成元 Drive URL・不採用案は Obsidian タスク `Tasks/po_agent/crypto-riddle/20260910_explore_background_scene.md` に記録。
- 色指定はダーク文脈トークンを日本語に翻訳（background/surface → 生成り色〜暖色寄りの暗い焦げ茶、primary → 金色をごく少量）。

**P-3（bg-s1-office・執務室・#140 で #132 の配色に作り直し・採用＝r3のE案）**

> 中堅システム会社の経理部の執務室を横長の構図で描いた背景イラスト。画面の上下左右の端まで室内の絵で埋め尽くし、白い余白・黒い帯・枠は一切入れない。画面の上部は、青白い蛍光灯の照明パネルと空調の吹き出し口が並ぶ天井が画面の上端まで続く。画面の下部は、床が手前に向かって画面の下端まで続き、下ろしたブラインドの隙間から入る外光が床に細い光の帯を落としている。時間帯は日中だが、窓のブラインドは下ろされ、天井の照明と机上のモニタの光が室内を照らしている。画面の中段に事務机があり、その上にノートパソコンと書類が置かれている。ノートパソコンの天板の外側は、ロゴもマークも何も無い真っ黒の無地。机に向かって黒の回転オフィスチェアに座る経理担当の男性が一人いる（20代後半〜30代前半の細身、黒髪の短めで前髪は目にかからない、硬く緊張した真剣な表情、白い長袖ワイシャツの第一ボタンを外しノーネクタイ、濃色のスラックス）。その左側に経理部長の男性が立っている（50代後半〜60代、白髪交じりのグレーの短髪を七三分け風、背が高めの細身で姿勢が良い、臙脂色のVネックニットベストに白い長袖ワイシャツとネクタイ、ベージュのスラックス、片手をポケットに入れ、もう片方の手を経理担当の方へ軽く差し出す）。二人の背後の壁際には書類ファイルの詰まった書棚があり、画面の右寄りの壁にサーバ室へ通じる灰色の金属製のドアが一つある（ドアに文字や表示板は付けない）。人物・机・書棚・ドアは画面の中段に余裕をもってまとめ、天井と手前の床の部分には人物や家具を置かない。文字・看板・ロゴは描かない。色は深い藍紫（ネイビーとパープルの暗色）を基調と陰影に、淡い水色がかった白をハイライトに用い、照明とモニタにネオンブルーの光をごく少量だけ差し色にする。近未来的なサイバーパンク調の緊張感がありつつ、実在しそうな会社の室内として描いたアニメ調イラストで、線は細く清潔。
>
> **運用メモ**: 否定の指示（「黒い帯を入れない」）は効かず、横長16:9では4回中4回、上下に黒い帯やスマホ風の操作アイコンが焼き込まれた。「上端まで天井・下端まで床を描く」と、上下に描くものを具体的に書いた3回目の作り直し（r3）で帯が消え、この案（E）を採用した。

**P-4（bg-s1-server・サーバ室・#140 で #132 の配色に作り直し・採用＝B案）**

> 中堅システム会社のサーバ室を横長の構図で描いた背景イラスト。両側にラックマウント型のネットワーク機器とサーバが並び、ケーブルが整然と配線され、機器の状態表示ランプが小さく灯る。中央奥に複数のモニタを並べた運用端末の机があり、その前に立って画面を確認している情報システム担当の男性が一人いる（短めの黒髪、白い半袖のワイシャツ、濃色のスラックス、やや背面寄りの姿）。運用端末の横の壁に、執務室へ通じる灰色の金属製のドアが一つある（ドアに文字や表示板は付けない）。窓のない室内で照明は控えめ。人物・機器・端末・ドアは画面内に余裕をもって配置し、画面の上部には天井の照明とケーブルラックを描いて操作ボタンを重ねられるようにする。文字・看板・ロゴは描かず、画面の縁に枠や帯を入れない。色は深い藍紫（ネイビーとパープルの暗色）を基調と陰影に、淡い水色がかった白をハイライトに用い、機器のランプとモニタにネオンブルーの光、状態表示ランプの一部にネオングリーンの光をごく少量だけ差す。近未来的なサイバーパンク調の緊張感がありつつ、実在しそうな会社のサーバ室として描いたアニメ調イラストで、線は細く清潔。
>
> **運用メモ**: #139 までの旧版はサーバ室の横版にドアが無く、「執務室への扉」ホットスポットは通路手前に仮置きしていた。本案（#140）で新たにドアを描き、横・縦とも実際に描かれたドアの位置へホットスポットを移した。

S1 探索背景4枚の配色作り直し（2026-09-15 確定・#140）: #132 のサイバーパンク配色（深い藍紫・淡い水色がかった白・ネオンブルー/グリーンの差し色）に合わせ、横・執務室（P-3）／横・サーバ室（P-4）／縦・執務室（P-13）／縦・サーバ室（P-14）の4枚を作り直した。構図・人物は既存プロンプトを踏襲し、色指定のみ置き換えた。生成元 Drive URL・不採用案は Obsidian タスク `Tasks/po_agent/crypto-riddle/20260914_s1_backgrounds_regenerate.md` に記録。9つのホットスポット座標（横・縦）は新しい絵を実測して更新した（`scenarios/s1-targeted-email-intrusion.yaml` 参照。新旧座標の一覧は Issue #140 の PR 本文に記録）。

S1 探索背景の縦版（2026-09-14 確定・#121）: image_agent 自作・9:16・各2案生成し代表が採用（サーバ室＝案A／執務室＝初回と再生成で「上下を余白に」の指示が白い帯・枠として焼き込まれ、ノートPCのロゴ・ドアの英字も出たため、上下に描くもの（天井の照明・空調、手前の床）を具体的に書いた3回目の案F を採用）。人物の外見は `docs/characters.md` §13.2（中野・経理部長 夏目・サーバ管理者）に準拠。横版と同じくドアを描き、door ホットスポットの置き場所にする。生成元 Drive URL・不採用案は Obsidian タスク `Tasks/po_agent/crypto-riddle/20260913_s1_portrait_backgrounds.md` に記録。

**P-13（bg-s1-office-portrait・執務室 縦・#140 で #132 の配色に作り直し・採用＝B案）**

> 中堅システム会社の経理部の執務室を、やや低い目線から少し見上げるように描いた縦長の構図の背景イラスト。画面の上部には、青白い蛍光灯の照明パネルと空調の吹き出し口が並ぶ天井が画面の上端いっぱいまで広がっている。画面の下部には、手前に向かって広がる床が画面の下端いっぱいまで続き、下ろしたブラインドの隙間から入る外光が床に細い光の帯を落としている。画面の中段に事務机があり、その上にロゴやマークの無い無地のノートパソコンと書類が置かれている。机に向かって黒の回転オフィスチェアに座る経理担当の男性が一人いる（20代後半〜30代前半の細身、黒髪の短めで前髪は目にかからない、硬く緊張した真剣な表情、白い長袖ワイシャツの第一ボタンを外しノーネクタイ、濃色のスラックス）。その左側に経理部長の男性が立っている（50代後半〜60代、白髪交じりのグレーの短髪を七三分け風、背が高めの細身で姿勢が良い、臙脂色のVネックニットベストに白い長袖ワイシャツとネクタイ、ベージュのスラックス、片手をポケットに入れ、もう片方の手を経理担当の方へ軽く差し出す）。二人の背後の壁際には書類ファイルの詰まった書棚があり、書棚の横にサーバ室へ通じる灰色の金属製のドアが一つある（ドアに文字や表示板は付けない）。人物・机・書棚・ドアは画面の中段にまとめ、天井と手前の床の部分には人物や家具を置かない。画面の上下左右の端まで室内の絵で埋め、白い余白・枠・帯は入れない。文字・看板・ロゴは描かない。色は深い藍紫（ネイビーとパープルの暗色）を基調と陰影に、淡い水色がかった白をハイライトに用い、照明とノートパソコンの画面にネオンブルーの光をごく少量だけ差し色にする。近未来的なサイバーパンク調の緊張感がありつつ、実在しそうな会社の室内として描いたアニメ調イラストで、線は細く清潔。（`--aspect-ratio 9:16`）

**P-14（bg-s1-server-portrait・サーバ室 縦・#140 で #132 の配色に作り直し・採用＝A案）**

> 中堅システム会社のサーバ室を縦長の構図で描いた背景イラスト。画面の上下左右の端まで室内の絵で埋め尽くし、白い余白・枠・帯は入れない。画面の上部は、天井の照明と天井から吊られたケーブルラックが画面の上端まで続く。画面の中段は、奥へまっすぐ伸びる通路の両側にラックマウント型のネットワーク機器とサーバが並び、機器の状態表示ランプが小さく灯り、ケーブルは整然と配線されている。通路の左側と右側のラックは、それぞれ前面が見えるように描く。通路の突き当たりに複数のモニタを並べた運用端末の机があり、その前に立って画面を確認している情報システム担当の男性が一人いる（短めの黒髪、白い半袖のワイシャツ、濃色のスラックス、やや背面寄りの姿）。運用端末の横の壁に、執務室へ通じる灰色の金属製のドアが一つある（ドアに文字や表示板は付けない）。画面の下部は、機器の光が映り込むつやのある床が手前に向かって画面の下端まで続く。窓のない室内で照明は控えめ。文字・看板・ロゴは描かない。色は深い藍紫（ネイビーとパープルの暗色）を基調と陰影に、淡い水色がかった白をハイライトに用い、機器のランプとモニタにネオンブルーの光、状態表示ランプの一部にネオングリーンの光をごく少量だけ差す。近未来的なサイバーパンク調の緊張感がありつつ、実在しそうな会社のサーバ室として描いたアニメ調イラストで、線は細く清潔。（`--aspect-ratio 9:16`）

S2 探索背景の運用メモ（2026-09-11 確定）: S2 も image_agent 自作・16:9・各2案生成し代表が各1案（執務室・サーバ室とも案A）を採用。実在企業/事案は特定できない類型に抽象化（架空）。生成元 Drive URL・不採用案は Obsidian タスク `Tasks/po_agent/crypto-riddle/20260911_scenario_s2_vpn_ransomware.md` に記録。

**P-5（bg-s2-office・S2 執務室・採用＝案A）**

> 中堅企業の情報システム部門の執務室を横長の構図で描いた背景イラスト。手前に管理用の事務机とデスクトップPC（管理端末）が置かれ、その近くに立つ情報システム担当の男性と、少し離れて立つ管理部門長の男性の計二人がいる。壁際には脆弱性対応やバックアップ運用の資料が並ぶ書棚がある。奥の壁にサーバ室へ通じるドアが一つある。時間帯は日中で照明は明るめ。色は生成り色を基調に、暖色寄りの暗い焦げ茶を陰影に用い、金色をごく少量だけ差し色にする。派手さを抑えた静かで硬派な推理アドベンチャーの雰囲気のアニメ調イラストで、線は細く清潔。人物・机・書棚・ドアは画面内に余裕をもって配置し、上に操作ボタンを重ねられるよう構図に余白を残す。（`--aspect-ratio 16:9`）

**P-6（bg-s2-server・S2 サーバ室・採用＝案A）**

> 中堅企業のサーバ室を横長の構図で描いた背景イラスト。両側にラックマウント型のサーバやネットワーク機器が並び、片側にバックアップ用サーバの筐体がある。中央付近に、点検に来た保守業者の作業服姿の男性が一人立っている。奥の壁に執務室へ通じるドアが一つある。窓のない室内で照明は控えめ。色は生成り色を基調に、暖色寄りの暗い焦げ茶を陰影に用い、金色をごく少量だけ差し色にする。派手さを抑えた静かで硬派な推理アドベンチャーの雰囲気のアニメ調イラストで、線は細く清潔。人物・機器・ドアは画面内に余裕をもって配置し、上に操作ボタンを重ねられるよう構図に余白を残す。（`--aspect-ratio 16:9`）

S3 探索背景の運用メモ（2026-09-11 確定）: image_agent 自作・16:9・各2案生成し代表が採用（執務室=案B〔S1/S2 と陰影トーンを揃えるため。案A は線画寄りで不採用〕／システム運用ルーム=案A）。架空 EC 事業者に抽象化。生成元 Drive URL・不採用案は Obsidian `Tasks/po_agent/crypto-riddle/20260911_scenario_s3_ec_card_leak.md` に記録。

**P-7（bg-s3-office・S3 EC事業者の執務室・採用＝案B）**

> 中堅のEC通販事業者の執務室を横長の構図で描いた背景イラスト。手前にEC運営担当者の事務机とノートパソコンが置かれ、その近くに立つEC運営担当の男性と、少し離れて立つ管理部門長の男性の計二人がいる。壁際には決済・セキュリティ関連の資料が並ぶ書棚がある。奥の壁にシステム運用ルームへ通じるドアが一つある。時間帯は日中で照明は明るめ。色は生成り色を基調に、暖色寄りの暗い焦げ茶を陰影に用い、金色をごく少量だけ差し色にする。派手さを抑えた静かで硬派な推理アドベンチャーの雰囲気のアニメ調イラストで、線は細く清潔。人物・机・書棚・ドアは画面内に余裕をもって配置し、上に操作ボタンを重ねられるよう構図に余白を残す。（`--aspect-ratio 16:9`）

**P-8（bg-s3-ops-room・S3 システム運用ルーム・採用＝案A）**

> 中堅EC事業者のシステム運用ルームを横長の構図で描いた背景イラスト。壁面に監視用の大きなモニタが複数並ぶ運用監視席があり、その前に立つ開発委託先の担当者の男性が一人いる。近くにサーバやネットワーク機器のラックがある。奥の壁に執務室へ通じるドアが一つある。窓のない室内で照明は控えめ。色は生成り色を基調に、暖色寄りの暗い焦げ茶を陰影に用い、金色をごく少量だけ差し色にする。派手さを抑えた静かで硬派な推理アドベンチャーの雰囲気のアニメ調イラストで、線は細く清潔。人物・機器・ドアは画面内に余裕をもって配置し、上に操作ボタンを重ねられるよう構図に余白を残す。（`--aspect-ratio 16:9`）

SL 探索背景の運用メモ（2026-09-11 確定・#76 仕上げ）: image_agent 自作・16:9・各2案生成し代表が採用（自社執務室＝ドアなし版／委託先ブース＝修正版）。実在企業・実在事案は特定できない類型に抽象化（架空、citation-policy §1）。採用画像は `assets/backgrounds/bg-sl-office.png`・`bg-sl-vendor.png` として確定し、`src/ui/components/explore/scene-explorer.tsx` の `BACKGROUND_SRC` に登録済み。自社執務室の採用案にはドアが描かれていないため、`scenarios/sl-consignment-breach.yaml` の scene-office 側にあった委託先ブースへの door ホットスポット（goto）は削除した（scene-vendor 側の「執務室への扉」door は残し、執務室→委託先はシーンタブ、委託先→執務室はドア＋シーンタブの両方で移動できる）。生成元 Drive URL・不採用案は Obsidian タスク `Tasks/po_agent/crypto-riddle/20260911_scenario_legal_new.md` に記録。

**P-9（bg-sl-office・法務 自社執務室・採用＝ドアなし版）**

> あるサービス事業者の法務・情報システム部門の執務室を横長の構図で描いた背景イラスト。手前に事務椅子が背もたれをこちら側に向けて置かれ、その正面の事務机の上に、画面が椅子（座る人）の方をまっすぐ向いた薄型のデスクトップPCモニタがある。キーボードとマウスは椅子とモニタの間。画面には委託先からの一次報告が表示。壁際に委託契約書のファイルが並ぶ木目の書棚。ドアや扉は描かず壁面のみ。人物は描かない。日中で照明は明るめ。色は生成り色を基調に暖色寄りの暗い焦げ茶の陰影、金色をごく少量差し色。静かで硬派な推理アドベンチャーのアニメ調イラスト、線は細く清潔。（16:9）
>
> ③''''' 代表FBで PC の向きを椅子側に修正・執務室のドアは除去（背景/ホットスポットとも）

**P-10（bg-sl-vendor・法務 委託先ブース・採用＝修正版）**

> 個人データ処理を請け負う委託先企業の作業ブースを横長の構図で描いた背景イラスト。中央やや左に通常のモニタとキーボード・マウスの運用端末デスク（クラウドストレージ管理画面）、その前に委託先の担当者の男性が一人立つ。右側にサーバ機器と資料バインダーの金属ラック。奥の壁に自社執務室へ通じる普通の木製ドア。窓は描かない。中央に大テーブルは置かない。落ち着いた室内で照明控えめ。色は生成り色基調＋暖色寄りの暗い焦げ茶陰影＋金少量。アニメ調・線は細く清潔。（16:9）
>
> 机内蔵ディスプレイ・高い窓・不自然な本棚・中央テーブルを代表FBで修正済み

対策室背景（導入）の運用メモ: 2026-09-14 確定・#139: 4人（霧島・橘・小鳥遊・新任）の部屋の間取りを決め、画面には部屋の一部（見える席は奥の2席）を切り取って映す構図とし、image_agent 自作で横＝間取り版 H・縦＝間取り版 J を採用した（間取りの詳細は下記 P-12 参照）。人物は描かない（霧島・橘・小鳥遊は立ち絵で重ねる。室長 日野は姿を見せない設定）。生成元 Drive URL・不採用案は Obsidian タスク `Tasks/po_agent/crypto-riddle/20260914_taskforce_room_regenerate.md` に記録。

（経緯: 初回は #122（2026-09-14 確定）で机4台の島だけの簡素な部屋の構図（横=案A／縦=再生成の案C）を採用したが、S1 の背景とのトーン統一・間取りの具体化のため #139 で間取り版に作り直した。当時の生成元 Drive URL・不採用案は Obsidian タスク `Tasks/po_agent/crypto-riddle/20260913_taskforce_room_background.md` に記録済み。）

**P-12（bg-hq-taskforce・横・#139 採用＝間取り版 H）**

> 4人（霧島・橘・小鳥遊・新任）の部屋。奥の壁＝机2台と壁掛けモニタ／入口側の壁＝机2台（画面に映らない）／左の壁＝窓／右の壁＝ホワイトボード／入口のそば＝紅茶のテーブル（小鳥遊）／奥の机に多肉植物（霧島）。室長 日野の席は置かない。カメラは入口側の壁の中央やや右から奥と左の窓を見る。昼の自然光の明るい部屋（配色統一の例外・代表決定 2026-09-14）。
>
> 中堅システム会社のセキュリティ対応室の一角を、入口側の手前の壁の中央やや右に立った目線の高さから、奥の壁と左の窓の方向を眺める横長の構図で描いた背景イラスト。左の壁一面が大きな窓で、ブラインドを半分下ろし、晴れた午後の暖かい日差しが斜めに差し込んで、奥の壁と机と床に光の縞を落としている。奥の壁沿いに白い天板の事務机が2台並び、各机にデスクトップPCのモニタ1台とキーボード、青い座面のグレーの事務椅子がある。机の上には背表紙に文字のないファイルが数冊立ててあり、小さな多肉植物の鉢植えがある。机の上方の壁には薄型モニタが上下2段に計6面取り付けられ、画面は紺色の地に緑と水色の線でネットワーク構成図と棒グラフと折れ線グラフだけを描く（読める文字は描かない）。右の壁には白いホワイトボードがあり、横に伸びる一本の時系列の線といくつかの印と矢印だけが描かれている（文字は書かない）。右手前には木目の小さな丸テーブルがあり、木のトレーに白い紅茶のティーポットとソーサー付きのティーカップ2客、多肉植物の鉢植えを置き、角度のついたデスクライトが灯っている。人物は一人も描かない。文字・看板・ロゴ・透かしは描かず、画面の縁に枠や帯を入れない。白い壁とグレーの床を基調に、窓からの暖かい金色の光と、モニタ画面の紺と緑を差し色にする。線は細く清潔で、光と影の階調をしっかり付けた塗りのアニメ調イラスト。（`--aspect-ratio 16:9`）

**P-12（bg-hq-taskforce-portrait・縦・#139 採用＝間取り版 J）**

> 4人（霧島・橘・小鳥遊・新任）の部屋。奥の壁＝机2台と壁掛けモニタ／入口側の壁＝机2台（画面に映らない）／左の壁＝窓／右の壁＝ホワイトボード／入口のそば＝紅茶のテーブル（小鳥遊）／奥の机に多肉植物（霧島）。室長 日野の席は置かない。カメラは入口側の壁の中央やや右から奥と左の窓を見る。昼の自然光の明るい部屋（配色統一の例外・代表決定 2026-09-14）。
>
> 中堅システム会社のセキュリティ対応室の一角を、入口側から奥の壁に向かってやや低い目線で見上げる縦長の構図で描いた背景イラスト。画面の上下左右の端まで室内の絵で埋め尽くし、白い余白・枠・帯は入れない。上部は天井の蛍光灯と空調の吹き出し口が上端まで続き、その下の奥の壁に薄型モニタが上下2段に計4面並ぶ（画面は紺色の地に緑と水色の線で図とグラフだけを描き、読める文字は描かない）。中段は、左端に大きな窓の一部とブラインドがあり、晴れた午後の暖かい日差しが斜めに差し込み、奥の壁沿いに白い天板の事務机が2台並び、各机にデスクトップPCのモニタとキーボード、青い座面のグレーの事務椅子がある。右端にはホワイトボードの一部が見え、線と印と矢印だけが描かれている。下部は、右手前に木目の小さな丸テーブルがあり、木のトレーに白い紅茶のティーポットとソーサー付きのティーカップ2客、多肉植物の鉢植え、角度のついたデスクライトを置き、光の縞が落ちたグレーの床が画面の下端まで続く。人物・文字・看板・ロゴ・透かしは描かない。白い壁とグレーの床を基調に、暖かい金色の光とモニタ画面の紺と緑を差し色にする。線は細く清潔で、光と影の階調をしっかり付けた塗りのアニメ調イラスト。（`--aspect-ratio 9:16`）

生成メモ: 見本画像を入力にした手直しは、構図を保って席数が変わらず、見本の透かしも残ったため採らず、間取りを決めてから書き起こしたプロンプトで新規生成した（2026-09-14）。

## プロジェクト固有ルール

- 世界観演出の display フォントサイズ（40/48/64）を見出しに限り許可（本文・操作UIは標準スケール）
- 見出し/世界観＝明朝、本文/操作UI＝ゴシックの使い分け
- **全画面が単一のダーク文脈**（2026-09-14 改訂・#132。旧「ゲームプレイ画面はダーク文脈、タイトル/メニュー/図鑑はライト文脈」は撤回。上記「基本方針」節のとおりライト文脈は廃止し、OS のライト/ダークモード設定にも追従しない）
- カード・スロット等のゲーム固有UIはカスタム可。ただし**ボタン・状態表示・エラー処理は本基準・DESIGN_STANDARDS に準拠**する
- 基準から逸脱が必要になったら、無断で逸脱せず本節に理由付きで追記してから実装する
